# Round 1 compression log — code-06-power-ups.md

One word-level cut; zero sentence cuts.

- Cut "at all" from "I wouldn't have started this many projects at all" (Superconductor) — redundant with "this many": "at all" negates completely, "this many" concedes some; removing it loses nothing and fixes the slight illogic.

Considered and kept:
- ", and it will get you if I don't say it" (gdoc gotcha) — carries a direct-address claim (you specifically will hit this), not filler; C2.
- "at all" in "You don't need these tips at all" — headline intensity on the lesson's main claim; C1.
- "maybe fifteen minutes", "mostly read-only" — calibrated hedges; C7 protected.
- "skip the rest" — imperative pair with "Do the one", rhythmic not padded.
- "That's the whole thing — no icons, no menus, just forty years..." — reads like restatement but carries three new facts.
- All four "The win:" closers — protected.

Every other sentence failed the deletion test in the good direction: each adds a new claim, feature, instruction, or caveat.
