Can any passage be rewritten to Pareto-dominate the original?

Two kinds of pass, run until neither changes anything:

1. **Pareto rewrites.** Fable subagents draft alternatives to each passage;
   separate judge subagents compare alternative against original across the
   `00-voice/style-criteria.md` dimensions and accept only strict improvements.
2. **Paul Graham compression.** A dedicated pass that cuts until no sentence
   (or word) can be removed without losing meaning. The one protected class:
   deliberate repetition that highlights a core message — per criterion C9,
   repetition survives only when it's verbatim incantation for intensity, and
   each instance must be justified against the lesson's headline message.
   Reworded restatement never survives. The final output should be exactly as
   tight as it deserves to be.

Store rounds here: per file, the alternatives, the judge verdicts, and what
the compression pass removed.
