# Consistency fixes — 2026-07-05

- code-01-install.md: title "# K1 — Install what the engineers use" → "# Install what the engineers use" — dropped the K1 prefix no other page carries.
- code-01-install.md: added the missing italic track line "*Code track, lesson 1.*" — every other lesson page has one.
- code-06-power-ups.md: title "# K6 — Power-ups" → "# Power-ups" — same prefix drift.
- code-01-install.md: cut "let's try and test them." from the debug quote — verbatim sprinkle also in code-05's Standing-instructions section, where the full curiosity loop is the lesson; kept there.
- cowork-03-trust-protocol.md: cut "— Cowork sometimes tucks files inside its virtual machine otherwise" — same aside (verbatim but for hides/tucks) in cowork-01, where it explains the prompt's last line at first contact; kept there.
- cowork-01-first-mess.md: "which is a computer inside your computer, where Claude runs the code" → "...a padded room where Claude runs the code" — cowork-04 ("the padded room from lesson 1") and cowork-05 call back to a term lesson 1 never coined.
- cowork-01-first-mess.md: "the chat tab" → "the Chat tab" — tab names are capitalized everywhere else (Chat, Cowork, Code).
