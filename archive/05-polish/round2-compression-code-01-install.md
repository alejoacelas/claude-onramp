# Round 2 compression log — code-01-install.md

1 cut.

- Cut: "Install the app, give it access to your computer files, and set up permissions so it's less likely to take unwanted actions:" — roadmap sentence pre-summarizing the numbered list that immediately follows; the permissions rationale is restated (better) in the post-list paragraph ("Auto mode means Claude stops asking permission for routine actions and interrupts you only for risky ones") and folder access is covered by the folder-access bullet. Classic throat-clearing per the anti-criteria ("In this section we'll…").

Considered and kept:
- "It doesn't." — resolves the reader-voiced objection it follows; two-word denial beat, not padded restatement.
- "the setting is named something close to 'Allow auto mode'" and "that usually clears it" — C7 calibrated hedges, protected.
- "Auto is still rolling out… pick 'Accept edits' instead" — reader-facing caveat, protected.
- "it works — and it works *for you*" — exact-repetition incantation, protected.
- Final paragraph — the win, protected.
