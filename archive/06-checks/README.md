Is every checkable claim in the guide verified, and which flags belong in the text?

Same diverge-then-select pipeline as the inventory, applied to factual risk.
All flags are blocking: the guide doesn't publish while any stands unresolved.

1. `flags-all.md` — the divergent pass: every claim in the drafts that could be
   wrong or drift (prices, plan gating, UI paths, commands, feature behavior,
   dates), with location, type, and what breaks for the reader if it's wrong.
2. `verdicts.md` — one online check per flag (Opus agents with web search):
   confirmed / wrong / unverifiable, the correction, the source, checked date.
3. Selection: wrong facts get fixed in the drafts; unverifiable ones get
   rewritten to not depend on the fact or downgraded to a hedged aside; of the
   confirmed ones, only the most relevant keep an in-text caveat — the rest are
   verified silently and recorded here.
