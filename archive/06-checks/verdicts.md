# Flag verdicts — checked online 2026-07-04

One Opus web-check per flag (internal flags checked against the repo). Columns: flag id (see flags-all.md for the claim), verdict, checker confidence, correction (if wrong), source, note.

Summary: 92 confirmed, 15 wrong, 9 unverifiable, out of 116. Missing checks: none.

| flag | verdict | confidence | correction | source | note |
|---|---|---|---|---|---|
| F001 | confirmed | high |  | https://platform.claude.com/docs/en/about-claude/models/overview | Anthropic's platform docs confirm Claude Fable 5 (claude-fable-5) is the most capable widely released model, ranked above Opus 4.8 ("for workloads that need the highest available capability, use Claude Fable 5"). Available on claude.ai chat for Pro/Max/Team/Enterprise and explicitly in Claude Code as an agent harness. Precise name is "Fable 5"; "Fable" is fine as shorthand. Cowork is not named as explicitly as Chat/Code on the models pages, but Fable is offered across Anthropic's paid consumer surfaces, so the "across Chat/Cowork/Code" framing is substantively accurate. |
| F002 | confirmed | medium |  | https://support.claude.com/en/articles/11817273-use-claude-s-chat-search-and-memory-to-build-on-previous-context | All three parts hold: memory keeps an auto-generated summary of who you are (role, projects, preferences), Drive and Asana are both current official connectors, and Artifacts make quick websites/apps. One imprecision: docs never state the memory summary is "2 paragraphs" — length isn't specified and varies, so soften to "a short summary" rather than committing to two paragraphs. |
| F003 | confirmed | high |  | https://claude.com/resources/tutorials/claude-cowork-a-research-preview | Official Anthropic/Claude sources confirm Cowork spawns parallel subagents on user request. The claude.com Cowork research-preview tutorial states "Cowork can break complex tasks across independent sub-agents that work in parallel, each with fresh context" and gives user-request examples like "Spin up subagents to research each one's pricing...". The Claude Help Center's "Get started with Claude Cowork" adds that Claude "coordinates parallel workstreams... through sub-agent coordination," using "the same agentic architecture that powers Claude Code." The exact wording 'Claude, use subagents!' is illustrative rather than a fixed command, but the underlying capability (many Claude clones available on command) is accurate. Note: Cowork is a research preview and cross-session memory is limited, but that doesn't affect this claim. |
| F004 | unverifiable | medium |  | https://support.claude.com/en/articles/14479288-claude-cowork-desktop-architecture-overview | The official "Claude Cowork desktop architecture overview" confirms two-thirds of the claim: code execution runs in "an isolated virtual machine (VM)" sealed from the host OS by the platform hypervisor (Apple Virtualization.framework / Hyper-V / QEMU), with network egress filtering and syscall restrictions — i.e. its own restricted computer. But no authoritative Anthropic doc states the VM is ephemeral or "reset between tasks"; the get-started and architecture articles are silent on VM lifecycle/state persistence (they only note that files are delivered to your connected folders, and that tasks stop if the app closes). The "ephemeral" attribute is supported only by third-party reverse-engineering (Pluto Security), which found session disks formatted fresh and CA certs regenerated each VM boot — plausible but not authoritative. Recommend keeping "restricted" / "own computer" as stated and softening or dropping "ephemeral" unless Anthropic's Trust Center security overview documents it. |
| F005 | confirmed | high |  | https://code.claude.com/docs/en/plugins | Official Claude Code docs confirm the substance of the claim. Claude Code is extensible via plugins that bundle skills, agents, hooks, MCP servers, LSP servers, and background monitors — a wide, open-ended surface. Anyone can create a plugin (quickstart shows building one from scratch) and share it: distribute via a git-repo marketplace (`/plugin marketplace add <repo>`), or submit to Anthropic's public `claude-community` marketplace after review. There's also `claude-plugins-official`. So "infinitely customizable extensions that anyone on the internet can create and share" is accurate marketing-flavored phrasing over a genuine capability; "infinitely" is hyperbole but not misleading. The surrounding draft line "restricted by default" also matches Claude Code's permission model. |
| F006 | confirmed | high |  | https://www.anthropic.com/news/introducing-claude-tag | Anthropic's official announcement confirms "Claude Tag" — Claude joins Slack channels as a persistent, shared teammate that anyone can delegate work to by typing @Claude. Full product name is "Claude Tag" (the guide may want to use that on first mention). Two scope nuances worth reflecting: it's in beta for Claude Team and Enterprise customers only, and it replaces the older "Claude in Slack" app (admins opt in within 30 days). Note also there's a separate, distinct "Claude Code in Slack" product — don't conflate the two. |
| F007 | confirmed | high |  |  | front-door.md line 35 links [the appendix](../appendix-tag.md), yet appendix-tag.md sits beside it in 04-drafts/, so the link resolves to a nonexistent repo-root file; no published directory layout exists anywhere (05-polish/ is empty except its README) that would make the ../ prefix valid. The link should be appendix-tag.md unless a final structure that separates the files is defined. |
| F008 | wrong | high | Only Cowork's shell/code-execution runs inside the isolated Linux VM. Computer use has "no sandbox between Claude and what's on your screen," and the agent loop, file reads/writes in connected folders, web fetches, connectors, and local MCP servers run natively on the host outside the VM. So a slip or prompt injection CAN reach beyond the padded room: control your real desktop, act through authenticated connectors, and write files in mounted folders. | https://support.claude.com/en/articles/14479288-claude-cowork-desktop-architecture-overview | Anthropic's own docs contradict the fully-sealed-in-a-VM framing. The architecture overview says only shell commands and code Claude writes execute inside the isolated Linux VM; the agent loop, file reads/writes in connected folders, web fetches, and local MCP servers run natively on the device. The Use Claude Cowork safely page explicitly states computer use has no sandbox between Claude and what is on your screen, and independent security analyses note browser/Chrome control and connectors run outside the VM with real sessions and the user's permissions. The VM claim is partially true (code execution is isolated) but the sweeping cannot-reach-your-machine reassurance is false; reframe to note computer use, connectors, and connected-folder access as real host reach-outs. |
| F009 | unverifiable | medium |  | https://support.claude.com/en/articles/13345190-get-started-with-claude-cowork | No authoritative Anthropic/support source states any setup or time-to-first-task figure. I fetched the official "Get started with Claude Cowork" support article, the "Cowork desktop architecture overview" article, and Anthropic's own three-steps tutorial — none give minutes, and none even quote the ~2GB VM download; the tutorial page's only "5 min" is the video length. So "15 minutes" can't be confirmed or contradicted from an authoritative source, and I can't time a real run here (no Cowork install). Closest documented facts, all third-party (claudecowork.im): the app downloads a ~2GB Linux VM that "may take several minutes depending on your connection," with overall first-time setup estimated at "~30 minutes" including app download + subscribing + VM download. The two drafts frame the 15 minutes differently and this matters: cowork-01 starts the clock "after the install finishes" (i.e., excluding the download), for which producing a folder inventory + plan in under 15 min is generous and plausible; front-door's "within fifteen minutes of installing" is ambiguous and, if it's read to include the ~2GB download, risks running optimistic against the ~30-min total. Suggest either aligning front-door to the "after install finishes" framing or softening to a hedge ("about fifteen minutes, once the download's done"), since download time is connection-dependent. |
| F010 | confirmed | high |  | https://www.anthropic.com/product/claude-code | Anthropic's own product page states "At Anthropic, the majority of code is now written by Claude Code. Engineers focus on architecture, product thinking, and continuous orchestration." Anthropic also published "How Anthropic teams use Claude Code," documenting internal use across ~10 departments (data infra, security engineering, product, inference, data science, etc.). So the load-bearing part — Anthropic engineers use Claude Code for their own jobs — is solidly confirmed. Caveat: the softer editorial flourish "the thing they put the most effort into" is not stated verbatim by any authoritative source (no page ranks Claude Code as their single highest-effort project). It's a fair characterization given how central it is, but if the guide wants it airtight, lean on the sourced framing ("engineers use it for their own work / most of Anthropic's code is now written by Claude Code") rather than the "most effort" claim. |
| F011 | confirmed | high |  | https://support.claude.com/en/articles/13345190-get-started-with-claude-cowork | The official Cowork support article and claude.com/pricing both state Cowork is available on paid plans only (Pro, Max, Team, Enterprise), and Pro includes both Claude Code and Cowork. Free includes neither. The flagged "Team Standard exclusion risk" did NOT materialize: the Cowork support article and the dedicated Team/Enterprise Cowork article treat "Team" as a single category with Cowork access, drawing no Standard-vs-Premium distinction. So "Cowork needs a paid plan — Pro or up" holds exactly as stated. As of Jan 16, 2026 Pro is priced at $20/mo (one third-party page cited $17 — ignore, not authoritative). |
| F012 | confirmed | high |  | https://claude.com/pricing | claude.com/pricing lists Claude Pro at $20/month billed monthly, or $17/month with annual billing ($200 up front). The "~$20/month" claim matches the monthly price exactly. |
| F013 | confirmed | high |  | https://claude.com/pricing | Fetched https://claude.com/pricing directly; it resolves with no cross-host redirect and renders the official Claude plans/pricing page (Free $0, Pro ~$17/mo annual, Max from $100/mo, plus Team and Enterprise tiers). |
| F014 | confirmed | high |  | https://claude.com/download | Fetched https://claude.com/download directly. It serves the Claude desktop app with "Download for macOS" (universal DMG) and "Download for Windows" (x64 and ARM64) buttons, plus Linux beta and ChromeOS. The page bundles Chat, Claude Cowork, and Claude Code. Authoritative (claude.com, Anthropic's own domain), so the claim holds exactly as stated for Mac and Windows. |
| F015 | wrong | high | Cowork is opened by clicking the "Cowork" tab at the top center of the desktop app (one of three tabs: Chat, Cowork, Code) — not in the left sidebar. The left sidebar manages parallel sessions/tasks, not mode switching. | https://code.claude.com/docs/en/desktop-quickstart | The label "Cowork" is right, but the location is wrong. Two authoritative sources agree it's a top tab, not a left-sidebar item: the Claude Code desktop quickstart says "The desktop app has three tabs: Chat, Cowork, Code" and "Click the Code tab at the top center" (the tabs sit together at top center). The support.claude.com Cowork article describes a top mode selector with Chat and Cowork tabs. The left sidebar exists but is for managing parallel sessions, a distinct function. Fix the guide to say "click the Cowork tab at the top of the app." |
| F016 | confirmed | medium |  | https://support.claude.com/en/articles/12622703-deploy-claude-desktop-for-windows | Anthropic's Windows deploy article confirms the exact feature name "Virtual Machine Platform" and that Cowork requires it enabled. The claim's remedy is correct: Virtual Machine Platform is a genuine checkbox in the "Turn Windows features on or off" dialog and enabling it needs a restart — this is standard Windows behavior. Nuance: the official doc itself gives a PowerShell command (Enable-WindowsOptionalFeature -Online -FeatureName VirtualMachinePlatform -All -NoRestart), not the GUI checkbox path. Both work; the GUI path is arguably friendlier for the guide's non-technical audience. No Anthropic page documents the exact "Turn Windows features on or off" wording, but the path is factually valid, so the claim is not wrong. |
| F017 | confirmed | medium |  | https://github.com/anthropics/claude-code/issues/61863 | Official Anthropic docs (support.claude.com Cowork/Desktop articles) describe the on-device VM but never state a size. A fresh-install measurement in Anthropic's own claude-code repo (issue #61863) shows the downloaded compressed VM image rootfs.img.zst is ~2.0 GiB, matching "about 2 GB"; multiple community setup guides also report a ~2 GB first-run VM download. One nuance the guide should note: the ~2 GB download decompresses to ~9-10 GB on disk, so ~10 GB free space is the real requirement even though the download itself is ~2 GB. Medium confidence because the exact size isn't in official docs and the app can silently grow the on-disk footprint. |
| F018 | confirmed | high |  | https://claude.com/resources/tutorials/navigating-the-claude-desktop-app | Anthropic's own desktop-app tutorial describes Chat as a standalone mode — "the same Claude you know from claude.ai, plus quick entry, screenshots, dictation" — separate from Cowork and Code. Cowork's first-run "setup" is just the ~2GB VM download/update; only Cowork and the Code tab require a paid plan, and Chat is presented as available regardless of Cowork adoption. So on a paid account you can use Chat (including pasting/screenshotting an install error) before Cowork finishes setting up. The support Cowork article didn't explicitly address the pre-setup window, but the tutorial settles it; nothing contradicts the claim. Minor drafting note: the draft calls it the "chat tab" lowercase — the UI label is "Chat". |
| F019 | wrong | high | The folder bounds Claude's access to your local files — it can only read and write files inside the folder(s) you attach, and it can't attach more on its own (only you can, through the folder picker). But "nothing else" overstates it: within the same session Claude can also reach beyond that folder via connectors/MCPs you've enabled (Gmail, Drive, Slack, etc.), web search and web fetch, and it has its own sandbox scratch space. So the folder is the boundary for your filesystem, not for everything Claude can see and touch. | https://claude.com/docs/cowork/3p/local-access | The filesystem half of the claim is correct and well-documented: "Claude can only read and write files in folders you've connected," the agent can read/create/modify anything inside attached folders and nothing outside, and "the agent cannot attach a network-drive path on its own; only the user can, through the folder picker. This is a security boundary." But the absolute "nothing else is" is contradicted: connectors/MCPs, web search/fetch (internet), and a sandbox scratch space all extend Claude's reach past the picked folder. Network access separately follows configured egress settings. Recommend narrowing the claim to files/filesystem rather than "all Claude can see and touch." |
| F020 | confirmed | high |  | https://github.com/anthropics/claude-code/issues/47179 | Confirmed and still a real failure mode as of July 2026. Anthropic's own support docs state Cowork runs code/shell in an isolated virtual machine and advise users to "review the output location Claude specified when completing the task" (support.claude.com/en/articles/13345190 and .../13364135). Anthropic's own issue tracker (anthropics/claude-code #47179) confirms that by default Cowork writes outputs to a deeply nested internal session path (e.g. AppData\Roaming\Claude\local-agent-mode-sessions...\outputs) that can be cleaned up and become inaccessible ("Location is not available"), with no persistent default output folder — so users must manually connect/specify a folder each session. Minor nuance for the guide: the files aren't truly "hidden inside the VM" but land in an internal per-session app directory rather than the user's chosen folder; telling Claude the full destination path (or connecting the target folder up front) is the correct fix, exactly as the claim advises. |
| F021 | confirmed | high |  | https://code.claude.com/docs/en/interactive-mode.md | Confirmed for both surfaces. Claude Code interactive-mode docs list "Ctrl+V or Cmd+V (iTerm2) or Alt+V (Windows and WSL): Paste image from clipboard," inserting an [Image #N] chip in the input. The desktop app tutorial (claude.com/resources/tutorials/navigating-the-claude-desktop-app) and support article "Custom visuals in chat and Cowork" support pasting/screenshotting images into chat/Cowork. Minor nuance worth reflecting: in the Code terminal input the paste shortcut is typically Ctrl+V, not Cmd+V (Cmd+V only in iTerm2), so "paste straight in" is right but the exact key varies by terminal. |
| F022 | wrong | medium | Neither product uses a clickable "microphone icon in the chat input bar" for dictation. In Claude Code (CLI) you enable dictation by running /voice, then hold (or tap) the Space bar to record — there is no mic icon (the VS Code extension lacks one too; feature request #26113 to add it was closed unimplemented). In Claude Cowork / Claude Desktop on Mac, dictation is the quick-entry Caps Lock shortcut (enable in Settings > General), not a mic icon; the documented sound-wave symbol is "voice mode" (a two-way spoken conversation), not dictation. | https://code.claude.com/docs/en/voice-dictation | Official Claude Code docs are explicit that CLI dictation is /voice + hold/tap Space with no mic icon, so the code-02-delegate.md instance is definitively wrong. The Cowork getting-started support article documents no in-app mic/voice UI, and Claude Desktop dictation is the Caps Lock quick-entry shortcut — so cowork-02-talk-to-it.md is also unsupported by authoritative sources (third-party blogs claim a Cowork mic button, but Anthropic docs don't corroborate). Confidence is medium only because the live Cowork GUI could have an undocumented mic control; the Claude Code side is high-confidence wrong. |
| F023 | confirmed | high |  | https://support.claude.com/en/articles/14479591-use-dictation-in-office-agents | Claude's dictation shows live transcription, not transcribe-on-stop. The official dictation support article says "Your words appear in the composer as you talk" and "The transcribed text appears in the composer in real time." The Claude Code voice-dictation docs corroborate: "Your speech appears in the prompt as you speak, dimmed until the transcript is finalized." So the lesson's "watch your words land as text" is accurate. Minor nuance for the guide: the transcript may show dimmed/provisional until finalized, and exact interim-word behavior can vary slightly by surface (desktop app vs Office agents vs CLI), but all confirm words appearing while speaking. |
| F024 | confirmed | high |  | https://support.claude.com/en/articles/8664678-change-the-model-effort-and-thinking-settings | Anthropic's support doc ("Change the model, effort, and thinking settings") states the model/effort selector sits next to the send button in the chat input bar; you click the model name to switch, with a "More models" option for the full list — matching "model picker by the input bar." The available-models list includes Opus 4.8 (the current top model), so "pick the best offered" is sound guidance. One caveat, not a contradiction: in Cowork spaces the model selector can be greyed out or org-locked (e.g. an admin default or allowlist), so a beginner may not always be able to change it — worth a hedge if the guide implies it's always free to pick. |
| F025 | confirmed | high |  | https://wisprflow.ai/ | Fetched wisprflow.ai directly. Product is alive and current, providing AI voice dictation "in every application on your phone or computer" (Mac, Windows, iPhone, Android) — i.e. system-wide dictation everywhere. URL is correct. Recently raised $81M for their "Voice OS," so actively developed. Claim stands as written. |
| F026 | confirmed | high |  | https://www.anthropic.com/research/long-running-Claude | Anthropic's own research page ("Long-running Claude for scientific computing") documents this as "agentic laziness": "current models can suffer from agentic laziness—when asked to complete a complex, multi-part task, they can sometimes find an excuse to stop before finishing the entire task." Their Ralph-loop pattern exists precisely to catch it — when kicked back and asked "are you really done?", the agent "will admit if the task is not up to spec and continue working." This matches the claim that Claude reports completion ('done') when it actually only attempted/partially finished. It's corroborated by many current claude-code GitHub issues (e.g. #6049 "falsely marking incomplete work as COMPLETED", #14947 "marks tasks complete without verifying implementation", #12369). So the framing "says done when it means attempted / quietly skipped rows" is accurate and current. Minor nuance: the model isn't necessarily lying so much as stopping early and over-reporting; the guide's user-facing framing is fair. |
| F027 | confirmed | medium |  | https://www.datacamp.com/tutorial/claude-cowork-tutorial | Cowork does display files on command, but via an in-app preview/Artifacts panel on the right — not by opening them in the user's native OS apps across the VM boundary. DataCamp's tutorial: "Any files Claude reads or creates appear in the Artifacts pane (you can click on them to preview)"; a generated PDF previews inside Cowork "rather than launching in the system's default PDF reader." anthropics/claude-code issues #33499 and #51587 confirm the preview panel renders PDF/DOCX/HTML and auto-opens/focuses on Write/Edit. The guide's phrasings ("open the file at the end", "open the table next to the first PDF") work as verification prompts; just note the file appears in-app, not in a native app. |
| F028 | confirmed | high |  | https://support.claude.com/en/articles/13345190-get-started-with-claude-cowork | Official Cowork docs confirm that while shell commands/code run in an isolated VM, files Claude creates or modifies in a folder you connect are written to the actual folder on your computer ("Delivers finished outputs directly to your file system"; "Claude can read from and write to your local files"). Because those files live on the real host filesystem, any future session that connects the same folder will find them. Note a distinction the guide should keep clean: conversational memory is NOT retained across standalone Cowork sessions (only within projects), but saved FILES persist regardless — the claim is about files, so it holds. |
| F029 | confirmed | high |  | https://www.anthropic.com/engineering/effective-harnesses-for-long-running-agents | Anthropic's own engineering docs describe exactly this pattern as a first-class, recommended technique: a running progress/log file (they call it claude-progress.txt or CHANGELOG.md) that the agent keeps updated as it works — "the agent's portable long-term memory... acting as lab notes," tracking current status, completed tasks, skipped/failed approaches, and anomalies. Anthropic bakes it into their long-running agent harnesses, and Claude Code best-practice docs echo it (plan/progress markdown files updated during work). The guide's claim that asking for a running markdown log, updated as it works, is a reliable pattern is well-supported. Minor nuance: the strongest documentation is in the Claude Code / long-running-agent context rather than Cowork specifically, but the underlying model behavior (writing and updating a markdown file in the working folder) is the same; the guide's phrasing and example prompt are sound. |
| F030 | confirmed | high |  | https://claude.com/blog/subagents-in-claude-code | Anthropic's own subagent guidance treats verbose/long output as the default and tells users to "Specify what should be returned... Naming the output format helps Claude deliver it" (e.g., "Return a summary of each, not the full file contents"). The sub-agents doc frames subagents as work that "would flood your main conversation with... file contents." The behavior warning is current and earned. Note: "book-length" is rhetorical, not a literal documented figure, but the specify-a-shape-or-get-a-dump behavior is accurately described. |
| F031 | confirmed | high |  | https://claude.com/resources/tutorials/navigating-the-claude-desktop-app | Anthropic's "Navigating the Claude desktop app" tutorial states: "You can run multiple tasks at once, each in its own conversation, and switch between them from the sidebar." That confirms the side-by-side / start-one-then-start-the-next behavior in F031. Caveat: Anthropic docs don't name the exact "new task" control; the get-started support article references a "Tasks" list, and third-party sources call it "+ New task" (upper left), but the draft's generic "open a new task" wording is accurate and safe. Note the desktop app must stay open for tasks to keep running (matches the draft's other flags). |
| F032 | confirmed | high |  | https://support.claude.com/en/articles/13345190-get-started-with-claude-cowork | The official "Get started with Claude Cowork" support page confirms each task runs in its own isolated VM/session with its own context ("own attention span"), that tasks operate independently ("don't wait for each other" — Claude can coordinate multiple workstreams in parallel), and that context/memory is not shared across separate standalone sessions ("don't share notes"). One nuance the guide should be careful about: memory IS shared within a single Cowork *project* (persists across tasks in that project), so the "don't share notes" claim is accurate at the level of separate/standalone tasks but not for tasks inside the same project workspace. As a simplified beginner statement about running many independent tasks, it is accurate. |
| F033 | confirmed | high |  | https://support.claude.com/en/articles/13364135-use-claude-cowork-safely | Cowork's permission selector has two modes: "Ask before acting" and "Act without asking." The latter makes Claude work without pausing for approval on routine steps — the mode the claim describes. Both support.claude.com articles (Get started with Claude Cowork; Use Claude Cowork safely) confirm it, and both note that even in this mode Cowork still requires explicit permission before permanently deleting files, matching the draft's "Deletion still waits for your yes either way." If the guide wants to name it, the exact label is "Act without asking." |
| F034 | confirmed | high |  | https://support.claude.com/en/articles/13364135-use-claude-cowork-safely | The official "Use Claude Cowork safely" help page states Cowork requires explicit permission before permanently deleting any files ("You'll see a permission prompt and must select 'Allow' before Claude can perform deletion tasks"), and this deletion protection is a core safeguard that is not suspended by the "Act without asking" (auto-approve) mode. So F034 is accurate: deletion still waits for your yes even with auto-approve on. Note the doc's exact mode name is "Act without asking," not "auto-approve" — the draft's paraphrase is fine but the precise UI label differs. |
| F035 | wrong | medium | Update the framing: prompt injection is well documented in research AND, as of 2026, documented occurring in the wild and growing — no longer safely called "rare in practice." Google reported a ~32% relative rise in malicious injection content across crawled pages between Nov 2025 and Feb 2026; OWASP's 2026 Agentic Top 10 ranks goal hijacking #1; Anthropic itself calls it "far from a solved problem" and a "meaningful risk," saying no browser agent is immune. It's still not yet widespread against typical individual users (early-stage, no coordinated campaigns, and newer Claude models cut adaptive success to ~1%), so the honest reassurance is "an ordinary user's odds of being hit today are still low, but it's a real and rising risk you should follow the trust protocol for" — not "rare, don't organize your life around it." | https://www.helpnetsecurity.com/2026/04/24/indirect-prompt-injection-in-the-wild/ | Part 1 (well documented in literature) is solidly true. Part 2 ("rare in practice") is now contested/outdated as of the 2026-07 publish date: multiple 2026 sources document indirect prompt injection in the wild and growing, OWASP ranks it the #1 agentic risk, and Anthropic's own guidance refuses to characterize it as rare, calling it an unsolved, meaningful risk. It remains uncommon against a typical individual user for now, so "wrong" is about the reassuring "rare / not worth worrying about" framing, not a claim that attacks are widespread — hence medium confidence. Recommend softening to acknowledge it's real and rising while noting individual odds are still low. |
| F036 | confirmed | high |  | https://support.claude.com/en/articles/13345190-get-started-with-claude-cowork | Official Anthropic support docs confirm Cowork runs locally in an isolated VM on the user's own machine, not the cloud. The "Get started" page states: "The Claude Desktop app must remain open and your computer must be awake for Claude to work on tasks. If you close the app or your computer goes to sleep, active tasks will stop." The scheduled-tasks page adds: "Scheduled tasks only run while your computer is awake and the Claude Desktop app is open... Cowork will skip the task, then run it automatically once your computer wakes up." There is no cloud continuation for Cowork itself (that requires switching to Claude Code cloud routines). The claim as stated is accurate. |
| F037 | confirmed | medium |  | https://support.claude.com/en/articles/13947068-assign-tasks-from-anywhere-in-claude-cowork | Anthropic's "Assign tasks from anywhere in Claude Cowork" support article confirms you can message/assign a task to Claude from your phone while it works on the desktop ("Message Claude from your phone... same conversation, wherever you reach it"), and desktop tasks run in parallel independently. Nuance the guide could reflect: mobile is a single persistent Dispatch thread — you can't open separate parallel threads from the phone, so a phone-sent task queues into that one thread (which matches the draft's word "queue") rather than spawning a new parallel task tab like the desktop does. The exact concurrency interaction (phone task vs. multiple running desktop tasks) isn't spelled out in docs, hence medium confidence. |
| F038 | confirmed | high |  | https://support.claude.com/en/articles/13345190-get-started-with-claude-cowork | Official Anthropic support page confirms: the field is at Settings > Cowork > Global instructions, and "you can give Claude standing instructions that apply to every Cowork session" regardless of which folder you're in. Note the field's exact label is "Global instructions" (desktop app only); folder instructions are the separate per-directory layer. The guide's "Settings > Cowork" path and "every session reads them whatever folder" behavior are both accurate. |
| F039 | confirmed | high |  | https://support.claude.com/en/articles/14553240-give-claude-context-claude-md-and-better-prompts | The official Claude Help Center article (the Cowork context guide) states verbatim: "CLAUDE.md is a plain markdown file that Claude automatically reads at the start of every session in that directory" — no manual reference or attachment needed. The file is named CLAUDE.md. The root CLAUDE.md loads at session initialization; subdirectory CLAUDE.md files load on demand. The guide could name the file (CLAUDE.md) for precision if it doesn't already. |
| F040 | confirmed | high |  | https://support.claude.com/en/articles/13837440-use-plugins-in-cowork | Both halves check out against official Anthropic sources. Auto-apply: the Agent Skills overview (platform.claude.com) says "Claude automatically uses them when relevant," and the teach-skills tutorial says Claude recognizes a skill just from a task mentioning its name/purpose. Slash-picker in Cowork: the official "Use plugins in Cowork" support article says "Type '/' or click the '+' button to see the available skills from your installed plugins, in chat and in Cowork." Caution: general web results conflate a separate "Microsoft Copilot Cowork" product, so I relied only on Anthropic pages. |
| F041 | confirmed | high |  | https://support.claude.com/en/articles/12512180-use-skills-in-claude | Official "Use skills in Claude" support article confirms both paths in Cowork: (1) upload a ZIP — Customize > Skills > "+" > Create skill > Upload a skill (a ZIP containing the skill folder with a SKILL.md); and (2) browse a built-in directory — Customize > "+" > Skills tab, where you browse and click Install for skills shared with you or org-wide. The companion "Browse skills, connectors, and plugins in one directory" article corroborates the unified in-app directory. Nuance: the directory surfaces skills colleagues/your org shared, not an open public marketplace, so "built-in directory" is accurate but it isn't a global public gallery of arbitrary third-party skills. |
| F042 | wrong | medium | Cowork's VM network egress is filtered but org-configurable, not a blanket GitHub block. The shipping default is "package managers only," and github.com/githubusercontent.com are part of that package-manager baseline (Go modules, npm/pip/cargo git deps resolve to GitHub), so install-from-GitHub steps generally work. Admins can also widen egress to specific domains or "all domains" (full internet minus a legal blocklist). GitHub is only unreachable if an admin has explicitly restricted egress to exclude it — not by default. | https://support.claude.com/en/articles/13455879-use-claude-cowork-on-team-and-enterprise-plans | Authoritative Anthropic docs confirm Cowork's default egress is "package managers only" (Team default has egress ON) with options for specific domains or all domains — so a categorical "VM can't reach GitHub" is wrong. No official Anthropic page enumerates github.com in the package-managers allowlist by name (the managed-agents doc names only PyPI/npm as examples), but multiple independent reverse-engineering analyses (pluto.security, PromptArmor) and the anthropics/claude-code egress bug-report cluster consistently treat github.com/githubusercontent.com as part of the package-manager defaults, which matches how npm/go/pip resolve packages. Recommend the guide reframe this as "GitHub is reachable by default; a locked-down org could block it" rather than "won't work there." Medium confidence because the github.com-specific inclusion rests on strong secondary evidence rather than an explicit Anthropic enumeration. |
| F043 | confirmed | medium |  | https://support.claude.com/en/articles/12512198-how-to-create-custom-skills | Anthropic's custom-skills doc confirms skills are edited conversationally: "When you're iterating on a skill with Claude in chat, you can edit the skill files directly where they open beside the conversation. Highlight the text you want changed, click 'Edit with Claude,' and type your request." In Cowork, installed plugins/skills also have a "Customize" button that spawns a task where Claude adjusts the skill's contents conversationally. Nuance: the documented flow is a dedicated skill-editing surface / "Edit with Claude" / "Customize" task rather than a seamless natural-language instruction ("add this as a non-negotiable rule") dropped into an ongoing work conversation — the guide's phrasing is slightly more casual than the docs, but conversational editing of skills is genuinely supported. |
| F044 | wrong | high | Cowork is not centrally fixed by Anthropic. Users add and enable their own connectors/MCP servers: individual Pro/Max users can add custom remote-MCP connectors themselves, on Team/Enterprise plans an Owner adds them for the org and users then individually connect, and each user toggles which connectors a given conversation can use. Anthropic does not decide the service list once for everyone. | https://support.claude.com/en/articles/11175166-get-started-with-custom-connectors-using-remote-mcp | Official Claude Help Center docs show custom connectors via remote MCP are available in Cowork; individual Pro/Max users add their own, Team/Enterprise Owners add for the org, and users enable connectors per-conversation via the "+" > Connectors toggles. The claim that Anthropic decides which services connect once for everyone is contradicted. Nuance: on Free plans and within Team/Enterprise, an admin/owner gates what's available, so it's admin-managed there rather than fully self-serve — but it is never a single Anthropic-set list for all users. |
| F045 | confirmed | high |  | https://code.claude.com/docs/en/voice-dictation | Each Cowork concept has a documented Code counterpart. Folders: Code operates directly on your real files/folders. Dictation: built-in voice dictation via /voice in the CLI and agent view (v2.1.69+), tuned for coding vocab (code.claude.com/docs/en/voice-dictation). Trust protocol: Code's permission system asks before actions and lets you set what runs automatically (code.claude.com/docs/en/overview). Running many: parallel sessions/agents, e.g. via git worktrees and agent view. Reuse arc under different names: outputs saved as files, standing instructions in CLAUDE.md (read at every session start), and Skills (invoked by name or matched to task). One minor nuance worth noting, not a contradiction: Code's voice dictation requires a Claude.ai login (not API key) and a local mic, so it is unavailable in Claude Code on the web/remote — but the feature exists in the desktop app the guide points to. |
| F046 | confirmed | high |  |  | Line 56 of /Users/alejo/best/work/writing/claude-onramp/04-drafts/cowork-05-teach-it-once.md links "[Code track, lesson 1](code-01-install.md)", and code-01-install.md exists in the same 04-drafts/ directory, so the link resolves today. No final published structure exists yet (the old published guide was deleted 2026-07-04; 05-polish is empty), so the caveat stands that the link must be re-resolved if publishing renames or moves the lesson files. |
| F047 | confirmed | high |  |  | Verified in 04-drafts: code-01-install.md is titled "# K1 — Install what the engineers use" (no track/lesson subtitle) and code-06-power-ups.md is "# K6 — Power-ups", while all other lessons use plain titles plus a "*Code/Cowork track, lesson N.*" subtitle; cowork-05-teach-it-once.md line 56 cross-references code-01 as "[Code track, lesson 1](code-01-install.md)", matching the plain scheme rather than the K-prefix. The naming disagreement is exactly as claimed. |
| F048 | confirmed | high |  |  | Exactly two '[alts: ...]' placeholders remain in 04-drafts/front-door.md (lines 7 and 13) and one '[what's the key combo on Windows?]' in 04-drafts/code-01-install.md (line 16). A full grep of 04-drafts confirms no other non-link bracket placeholders, so the claim is complete and accurate. (For the Windows fix itself: the settings shortcut in the Claude desktop app is Ctrl+, on Windows.) |
| F049 | confirmed | medium |  | https://www.anthropic.com/engineering/claude-code-sandboxing | Accurate as a capability/scope framing: Claude Code runs under your own OS account, so its reach equals yours — Anthropic's docs confirm default read access to the whole computer and that it runs with the user's permissions. Caveat the guide should keep visible: this scope is gated, not free-by-default. Anthropic states Claude Code is "read-only" by default and "asks for permission before making modifications or running any commands" (only safe reads like cat/echo auto-allow); newer opt-in sandboxing further confines it to set directories/hosts. So "every program you can run, it can run" is a true ceiling but it asks first by default — the draft's next section (auto mode / permissions) already covers that, so the framing reads fine in context. |
| F050 | unverifiable | high |  | https://support.claude.com/en/articles/10065433-install-claude-desktop | No official Anthropic/Claude support page documents a keyboard shortcut for opening Settings in the Claude desktop app — the install page and the desktop quick-entry article only describe reaching it via Settings > General/Extensions. Cmd+, is the standard macOS Preferences convention and the Claude app is Electron-based, so it very likely works, and a third-party cheat sheet (fastshortcuts.com, not authoritative) lists Cmd+, for Mac and Ctrl+, for Windows. But no authoritative source confirms either, so per the UI-path rule this is unverifiable rather than confirmed. The bracket flagging Windows as unknown is appropriate; Mac's Cmd+, is plausible but undocumented. |
| F051 | unverifiable | medium |  | https://code.claude.com/docs/en/desktop | Official Desktop docs confirm Auto mode is enabled under Settings > Claude Code, but never give the exact toggle label "Allow auto permissions mode." They only state "Enable in your Settings → Claude Code." The precisely-documented parallel toggle is "Allow bypass permissions mode" for Bypass mode; the analogous auto toggle is more likely "Allow auto mode" (the mode is labeled just "Auto"), so the claim's wording is probably inexact but not confirmable from docs. Also note: auto mode is a gated research preview requiring Opus 4.6+/Sonnet 4.6+, and on Team/Enterprise an Owner must enable it — so the toggle isn't universally available to beginners. Checked code.claude.com/docs/en/desktop and /permission-modes plus the claude.com/blog/auto-mode announcement. |
| F052 | wrong | medium | The desktop app has three tabs — Chat, Cowork, and Code — and the docs say to open Claude Code by clicking the "Code" tab at the top center. Code is not directly next to Chat (Cowork sits between them), and the switcher is not described as a left-sidebar icon. | https://code.claude.com/docs/en/desktop-quickstart | Official quickstart: "The desktop app has three tabs: Chat, Cowork, Code" and "Click the Code tab at the top center." So the tab exists but the guide's position ("left sidebar") and adjacency ("next to Chat") are off — Cowork is between Chat and Code, and docs place the tabs top-center. UI is in flux: third-party coverage of a July 2026 redesign (miraflow, VentureBeat) describes a switcher moving into the sidebar with three icons, so a sidebar switcher may exist in newer builds, but the current authoritative doc says top center. Recommend rewording to "click the Code tab at the top" and noting three tabs (Chat, Cowork, Code) rather than asserting a left-sidebar icon next to Chat. |
| F053 | wrong | high | The environment dropdown's options are Local / Remote / SSH, not "Local vs Cloud or Default." The cloud option is labeled "Remote" (Anthropic-hosted cloud sessions), and there is no "Default" environment option — "default" is the config name of the "Ask permissions" permission mode, a separate dropdown. Correct framing: pick "Local" (vs "Remote" or an "SSH connection"); the folder/project picker and the Auto mode permission-mode option are both correct as stated. | https://code.claude.com/docs/en/desktop | Official desktop docs confirm three input-bar controls: Environment (Local/Remote/SSH), Project folder picker, and Permission mode (Ask permissions=default, Accept edits, Plan mode, Auto mode, Bypass). The folder picker and Auto mode parts of the claim are accurate. Only the environment-dropdown labels are wrong: it's "Remote," not "Cloud," and there is no "Default" environment. The guide draft (04-drafts/code-01-install.md line 19) tells beginners to pick "Local (instead of Cloud or Default)" — they'll actually see Local/Remote/SSH. |
| F054 | confirmed | high |  | https://code.claude.com/docs/en/permission-modes | The official permission-modes doc confirms the behavior: "Auto mode lets Claude execute without routine permission prompts. A separate classifier model reviews actions before they run, blocking anything that escalates beyond your request..." The modes table lists auto as "Everything, with background safety checks." Two nuances worth reflecting: (1) Auto mode is explicitly labeled a research preview ("reduces permission prompts but does not guarantee safety") — the check hint's research-preview flag is accurate. (2) Availability is now "All plans" per current docs (not gated to a single tier), though on Team/Enterprise an Owner must enable it first, and it requires Claude Code v2.1.83+ and a recent model (e.g. Opus 4.6+/Sonnet 4.6+). (3) Mechanism detail: rather than "interrupting" the user, the classifier blocks a risky action and Claude tries an alternative; it falls back to prompting the user only after 3 consecutive or 20 total blocks, or when an explicit ask rule matches. The guide's plain-language framing is accurate for beginners. |
| F055 | confirmed | high |  | https://code.claude.com/docs/en/quickstart | The official Claude Code quickstart (code.claude.com/docs/en/quickstart, Step 1-2) lists exactly these commands: macOS/Linux/WSL "curl -fsSL https://claude.ai/install.sh / bash" and Windows PowerShell "irm https://claude.ai/install.ps1 / iex" (Native Install, recommended). Then you run "claude" and are prompted to authenticate in your browser on first use. All three parts of F055 match verbatim. |
| F056 | wrong | high | The Code tab runs the same underlying engine as the CLI and shares configuration (CLAUDE.md, MCP servers, hooks, skills, plugins, settings, model access), but it is not just "the CLI with buttons, nothing else differs." Per the official docs, Desktop is interactive-only (no headless --print/--output-format), the dontAsk permission mode is CLI-only, several CLI flags have no desktop equivalent, third-party provider support (Bedrock, Google Cloud Agent Platform, Microsoft Foundry) differs, and CLI interactive shortcuts like Shift+Tab do not apply. Say "same engine and shared config, different interface with some capabilities the CLI has and Desktop doesn't (and vice versa)" rather than "nothing else differs." | https://code.claude.com/docs/en/desktop | Official Claude Code desktop docs confirm "Desktop runs the same underlying engine with a graphical interface" and shared config, but the same page's "Coming from the CLI?" and "Feature comparison" sections list concrete differences: headless/scripting flags (--print, --output-format) not available (interactive only), dontAsk mode CLI-only, some flags with no desktop equivalent, differing third-party provider support, and non-applicable terminal shortcuts. The "nothing else differs" wording is what makes the claim wrong. |
| F057 | unverifiable | medium |  | https://code.claude.com/docs/en/troubleshoot-install | No authoritative Anthropic source describes the claim's exact desktop/Cowork sign-in-loop fix (quit and reopen; if that fails, sign out of both browser and app and retry). I checked the Claude Code install/login troubleshooting docs, the "Log in to your Claude account," "Install Claude Desktop," and "Get started with Claude Cowork" support articles — none give this precise sequence. The closest documented fix is CLI-specific ("Reset your login": run /logout, close Claude Code, restart and re-authenticate). The claim's advice is plausible and consistent with general practice, but it is not stated as the recommended fix for auth loops on any current authoritative page, so I can't confirm it as written. Recommend softening to generic advice or citing the documented /logout reset for the CLI. |
| F058 | confirmed | high |  | https://github.com/anthropics/claude-code/issues/61233 | macOS TCC prompts Claude per-folder for protected directories (Desktop/Documents/Downloads/iCloud) — confirmed by official anthropics/claude-code issues #61233 and #7492, which show the sequence of per-folder "would like to access files in your X folder" dialogs. Denying blocks file access: issue #51984 documents that without the folder privacy grant Claude cannot read files (it actually crashes with an unhelpful "process exited with code 1"), and #32584 shows the same read-failure symptom. So the guide's "allow it, or Claude works blind" is accurate in substance. One nuance for the author: on the desktop app the prompt reads like a standard OS Files-and-Folders dialog, and a hard denial can surface as an opaque error rather than a graceful "blind" mode — but the practical instruction (allow the dialog or file reads fail) is correct. |
| F059 | confirmed | high |  | https://github.com/anthropics/claude-code/issues/55668 | The guide's only Cmd shortcut is Cmd+, (open settings); on Windows this is Ctrl+,. Anthropic's own claude-code GitHub issue #55668 states the Windows desktop app's expected shortcuts use Ctrl (or Win) where Mac shows ⌘, confirming the general Cmd-to-Ctrl swap the guide advises. The Cmd-to-Ctrl mapping is also the standard cross-platform application convention (e.g. new session ⌘⇧O maps to Ctrl+Shift+O). "Try Ctrl first" is the right first move; the fallback ("screenshot and ask") covers the rare exception. |
| F060 | wrong | high | The 30-day default is correct: Claude Code stores session transcripts locally under ~/.claude/projects/ for 30 days by default, controlled by cleanupPeriodDays (default 30, minimum 1). But to keep history longer you edit cleanupPeriodDays to a higher number in a settings file (~/.claude/settings.json), or run the /config command. There is no documented behavior where saying "keep all my conversation history" makes Claude change its own setting, and there is no "keep everything" value — 0 is rejected with a validation error (and historically disabled persistence entirely, the opposite of keeping all). Claude Code can edit settings.json only as an ordinary file with your permission, not as an automatic response to that phrase. | https://code.claude.com/docs/en/settings | Official docs confirm the 30-day default via cleanupPeriodDays (default 30, min 1; 0 is rejected). But the claim's mechanism is fabricated: no documented feature turns the phrase "keep all my conversation history" into a self-edited setting, and there is no value that keeps history indefinitely — you manually raise cleanupPeriodDays in ~/.claude/settings.json or use /config. Suggest rewriting the guide to tell beginners to edit cleanupPeriodDays (or use /config), not to rely on a magic phrase. |
| F061 | confirmed | high |  | https://support.google.com/docs/answer/12014036?hl=en | Google's support page confirms the menu path "File > Download > Choose a file type." Current text formats include Plain Text (.txt) and Markdown (.md) — Google added native Markdown export in mid-2024 (File > Download > Markdown (.md)). Either gives a plain-text copy droppable into a folder, so the claim holds as stated. Minor guide-improvement note: Markdown (.md) preserves headings/lists better than Plain Text for Claude to read, so the guide could nudge toward .md, but the claim is not wrong. |
| F062 | confirmed | high |  | https://platform.claude.com/docs/en/build-with-claude/pdf-support | Claude Code's built-in Read tool reads any file from the local filesystem, and its own tool description explicitly lists PDFs (.pdf), images (PNG/JPG), and Jupyter notebooks as supported. Files in the working folder are the default access scope. One nuance the guide could optionally note: for PDFs over ~10 pages the Read tool needs a page-range parameter, and there are size/page caps (~32MB, ~100 pages), but the claim as stated ('anything in the working folder, PDFs included') is accurate. |
| F063 | confirmed | medium |  | https://github.com/anthropics/skills/blob/main/skills/xlsx/SKILL.md | Sound as fallback advice: Claude Code's Read tool ingests CSV directly (plain text, no setup), while XLSX is binary and only works when Python (pandas/openpyxl) or the xlsx skill is available locally — so CSV is the more reliable export when a format resists. Minor staleness to flag: XLSX is no longer a dead end. Anthropic's official xlsx skill (anthropics/skills, covers .xlsx/.xlsm/.csv/.tsv) and openpyxl let Claude Code read/edit workbooks natively when the toolchain is present, so the parenthetical implication that XLSX is inferior is slightly overstated. The claim's conditional framing ('when a file format fights you') keeps it accurate; the author could soften it to 'if an XLSX gives Claude trouble, save it as CSV.' |
| F064 | confirmed | high |  | https://www.getaiperks.com/en/ai/claude-code-plan-mode | In context the guide tells Claude to "Write your plan as a file in this folder," so the plan is a plain markdown/text file. Claude Code plans are saved as .md files editable in any text editor (Ctrl+G opens them in your editor), confirming the file is just a plain-text document openable anywhere. No authoritative Anthropic docs page describes this precisely, but the behavior is well-established across guides and is inherent to markdown being plain text. Minor nuance: markdown opens cleanly in text editors; a beginner opening it in a rich word processor still sees the text, just with visible # symbols. |
| F065 | confirmed | high |  | https://support.claude.com/en/articles/11176164-use-connectors-to-extend-claude-s-capabilities | Such setups exist. (1) The native Google Drive connector is read-only — Anthropic's docs (claude.com/docs/connectors/google/drive) describe only reading, searching, and text-extraction; it has no create/delete capability, matching the connector's tool set which exposes no delete operation. (2) On Enterprise, connector Tool permissions (Customize > Connectors) are grouped as "read-only tools" vs "write/delete tools," and each category can be set to Always allow / Needs approval / Blocked; the docs give the exact example "Allow Claude to read files in Google Drive, but prevent it from creating or editing documents." Either is a concrete configuration under which Claude cannot delete Drive files. The only soft edge is the guide's "Claude can offer" framing (a behavior claim about what Claude will suggest), but the underlying configurations are real and current. |
| F066 | confirmed | high |  | https://support.google.com/drive/answer/2409045 | Google Drive does automatically keep version history for files — a genuine passive, zero-maintenance protection, so "snapshots your Drive already keeps" is accurate and does not overstate. One nuance the guide could optionally reflect: for uploaded (non-Google-native) files, a version can be auto-deleted after 30 days or once there are 100 newer versions, unless you mark it "Keep forever." Google-native Docs/Sheets/Slides keep richer, longer-lived history. So it's a real safety net but not infinite/guaranteed — fine as written for a beginner audience. |
| F067 | unverifiable | medium |  | https://code.claude.com/docs/en/checkpointing | No authoritative source describes what the naive phrasing reliably produces, so the behavior claim can't be confirmed as stated. What docs DO settle cuts against the easy reading: Claude Code's native "save-points" (checkpoints/`/rewind`) are explicitly NOT git, do not auto-commit, and don't track bash rm/mv/cp — "Checkpoints complement but don't replace proper version control." So "working git auto-commits" isn't a native feature; Claude must improvise git init PLUS an ongoing auto-save mechanism (a hook — it doesn't commit after each edit by default). `git init` + a first commit is likely; reliable ONGOING auto-commit ("save automatically as you go") in ~1 min is the unverified part. I ran a live headless test with the exact phrasing (`claude --print`, real binary); it was still running well past the claimed ~1 minute when I had to conclude, so the timing/reliability wasn't empirically confirmed. Recommend the guide either (a) hedge the "automatically as you go / about a minute" promise, or (b) point users at the native checkpoint/`/rewind` undo (which is real and instant) for the "undo everything" half, and treat ongoing git auto-commit as a separate, less-guaranteed setup. |
| F068 | confirmed | medium |  | https://git-scm.com/book/ms/v2/Getting-Started-First-Time-Git-Setup | Save-points = git is accurate; git is version control, and Claude Code's separate /rewind checkpoints "complement but don't replace" git per official docs (code.claude.com/docs/en/checkpointing), which the draft correctly treats as a distinct layer. The one real first-run friction the hint points at: git's first commit fails with "Please tell me who you are" unless user.name/user.email are set (unset on fresh machines). But Claude runs the git commands here, not the reader, so that identity config is Claude's to resolve non-interactively — it doesn't surface to the user as a git command they must learn, so "all you need to know" holds. Caveat for authors: this only stays true if Claude configures identity silently rather than asking the reader. |
| F069 | confirmed | medium |  | https://code.claude.com/docs/en/checkpointing | The mechanism and its rationale are authoritatively confirmed: Claude Code's checkpointing docs say it captures code state before each edit so you "can always return to a prior code state" and "pursue ambitious, wide-scale tasks" — exactly the logic that makes auto mode reasonable. In the guide, save-points = user-set-up git auto-commit, the standard robust version (within the folder it even recovers bash rm, since committed files restore). One caveat: "any change to your files" slightly overstates. Documented exceptions — gitignored files (e.g. .env, node_modules) are never committed so don't rewind; files outside the folder aren't covered; a not-yet-committed new file isn't captured and git reset --hard won't remove untracked files. The guide's next section already scopes out non-file/destructive actions, so the claim holds; consider softening "any" to "any tracked change in that folder" if precision matters. |
| F070 | confirmed | high |  | https://code.claude.com/docs/en/checkpointing | Official Claude Code docs confirm checkpointing: it automatically captures code state before each of Claude's edits and lets you undo/rewind. Two nuances a beginner-facing guide should reflect: (1) this is the Claude Code CLI feature, triggered by the /rewind command or pressing Esc twice on an empty prompt (a menu, not a conversational "ask it"), not a claude.ai app feature; (2) scope is limited to direct file edits made by Claude's editing tools — files changed by bash commands (rm, mv, cp) and external/manual edits are NOT tracked, and it complements rather than replaces Git. The claim's core behavior ("keeps checkpoints of its own edits; you can rewind what it just did") is accurate as stated. |
| F071 | confirmed | high |  | https://support.claude.com/en/articles/11049741-what-is-the-max-plan | Anthropic support confirms all elements: usage/rate limits cap "how much you can interact with Claude over a specific time period" and reset on time windows (rolling session windows plus weekly limits). The Max plan exists as the bigger, higher-usage plan in two named tiers, Max 5x ($100/mo, 5x Pro usage) and Max 20x ($200/mo, 20x Pro usage); paid plans buy more usage of the same models, so upgrading the plan (not downgrading the model) is the documented way to lift limits. All Claude surfaces (claude.ai, Claude Code, Cowork) draw on the same usage limit, which is why parallel sessions hit the cap faster. The "bigger plan, not worse model" phrasing is advice, but every factual premise it rests on is supported. |
| F072 | confirmed | high |  | https://code.claude.com/docs/en/desktop | Official Claude Code Desktop docs state exactly this. The keyboard-shortcuts table lists "Cmd N — New session," and the "Work in parallel with sessions" section reads: "Click + New session in the sidebar, or press Cmd+N on macOS or Ctrl+N on Windows, to work on multiple tasks in parallel." This is the correct binding for the Code tab of the Claude Desktop app, which matches the guide's context (running many parallel agents). Note: the general Claude web/chat app uses Cmd/Ctrl+O for a new chat, and quick entry on Mac is double-tap Option — but for the desktop Code sessions the guide is about, Cmd/Ctrl+N is right. |
| F073 | confirmed | high |  | https://code.claude.com/docs/en/sub-agents | Official docs confirm the behavior with no setup required: Claude Code ships built-in subagents (Explore, Plan, general-purpose) that "Claude automatically uses when appropriate," and the general-purpose one is used "when the task requires both exploration and modification... or multiple dependent steps" — exactly the "task too big for one session" case. Each subagent "runs in its own context window... works independently and returns results," which the main agent folds back — matching "hires helper Claudes and folds their work back together." Natural-language triggering works without config: "Claude automatically delegates tasks based on the task description in your request." One nuance for a beginner guide: delegation via natural language is a hint, not a hard guarantee ("Claude decides whether to delegate") — the guide's confident "it hires helper Claudes" is fine as plain-language, but strictly it's usually-but-not-always. |
| F074 | wrong | medium | In the Desktop Code tab, context usage is shown as a clickable "usage ring" next to the model picker (which sits at the bottom of the composer), not as an always-visible numeric token counter. You click the ring to reveal current context-window usage and plan usage; the readout is an abbreviated fraction/percentage (e.g. "161.3k / 200.0k (81%)"), not a spelled-out "300,000 of 1,000,000". | https://code.claude.com/docs/en/desktop | The authoritative desktop doc confirms a context-usage indicator exists in the Code tab session but describes it as a "usage ring next to the model picker" you click, not a passively-readable token counter "at the bottom." The core idea (an in-session context indicator exists) is right, but the guide's specifics — a visible token counter reading "300,000 of 1,000,000" — do not match the documented UI. The exact fraction format ("161.3k / 200.0k (81%)") comes from third-party writeups, not the official doc. The CLI/terminal does show a bottom status-line context indicator, but the guide targets the Desktop Code tab. Suggested rewording: "click the usage ring next to the model picker; it shows how full the context window is (e.g. 300k / 1M)." |
| F075 | confirmed | high |  | https://platform.claude.com/docs/en/build-with-claude/context-windows | The current default Claude Code models have a native 1M-token context window, so a counter reading "300,000 of 1,000,000" is accurate. Per platform docs, Claude Sonnet 5, Sonnet 4.6, Opus 4.8/4.7/4.6, and Fable 5 all have 1M windows, and "1M is the default: you don't need a beta header." The model-config doc confirms Claude Code's `sonnet` alias resolves to Sonnet 5 (native 1M) and `opus` to Opus 4.8 (1M). Nuance the guide could note ("1M for whom?"): older 200k-window models (e.g., Sonnet 4.5) would show "of 200,000," and the support page notes Pro subscribers may need usage credits enabled to reach 1M on Opus/Sonnet 4.6 — so the exact denominator depends on the model and plan. But for the current recommended default on paid plans, 1M is correct and current. |
| F076 | unverifiable | medium |  | https://platform.claude.com/docs/en/build-with-claude/context-windows | No authoritative Anthropic source names a specific handoff token count; docs describe "context rot" (quality degrades as the window fills) and offer compaction, but no "hand off at N" number. The 200K figure is a sane, conservative checkpoint relative to the 1M window (F075): independent testing puts measurable degradation on Sonnet 4.6 around ~400K tokens, so advising a handoff at ~200K errs safely on the early side and won't strand beginners. It's defensible advice, just not an official number — arguably conservative (practitioner guidance often waits until 50% / ~500K or ~80% fill). Keep as guidance, not a hard threshold. |
| F077 | confirmed | high |  | https://platform.claude.com/docs/en/docs/about-claude/glossary | Anthropic's official glossary defines tokens as "the smallest individual units of a language model, and can correspond to words, subwords, characters, or even bytes," with "a token approximately represents 3.5 English characters" for Claude. Since an English word averages ~4-5 characters, a token is a similarly-sized (slightly smaller) chunk of text — roughly 0.75 words/token, or ~1.3 tokens/word. The guide's plain-language simplification ("a fancy term for words, or similarly-sized chunks of text") is accurate and appropriate for a non-technical audience. It is fine for the counter math: reading "300,000 of 1,000,000" as roughly-that-many-words overestimates word count by ~33% but stays the right order of magnitude and doesn't affect the "past about 200,000, do a handoff" advice. No change needed. |
| F078 | confirmed | high |  | https://www.anthropic.com/engineering/effective-context-engineering-for-ai-agents | Anthropic's "Effective context engineering for AI agents" page describes "context rot": as context tokens grow, a model's ability to recall/attend to information decreases. It gives structural + ML reasons — the transformer attention mechanism's n² pairwise relationships get "stretched thin," and models have fewer parameters tuned for long-range dependencies because training data skews toward shorter sequences. It states this "emerges across all models" (a gradient, not a cliff), i.e. applies to current models, and frames the remedy as keeping context short rather than adding tokens — matching the guide's "nothing more tokens will fix." The claim is accurate as stated. |
| F079 | confirmed | high |  | https://code.claude.com/docs/en/memory | Official Claude Code memory docs state: "CLAUDE.md files are markdown files... You write these files in plain text; Claude reads them at the start of every session" and they are "loaded into the context window at the start of every session." Two tiny nuances for the guide: they're markdown files (markdown is plain text, fine for beginners), and more precisely they load "at the start of / at launch of" a session (injected as a user message before your first message) rather than literally "before the session starts." Core behavior claimed is accurate. |
| F080 | confirmed | high |  | https://code.claude.com/docs/en/memory | Official Claude Code memory docs list user instructions at ~/.claude/CLAUDE.md, scope "Personal preferences for all projects" (all projects / everywhere). Any folder can add its own CLAUDE.md: Claude walks up the directory tree loading each CLAUDE.md, and also discovers CLAUDE.md files in subdirectories (loaded on demand). Note the docs frame it as "user instructions" that apply everywhere rather than literally "global," but the path and behavior match the claim exactly. |
| F081 | confirmed | medium |  | https://code.claude.com/docs/en/memory | Official Claude Code memory docs confirm the behavior: "To add instructions to CLAUDE.md... ask Claude directly, like 'add this to CLAUDE.md,' or edit the file yourself via /memory." The guide's "add this to your instructions"/"add this to your global instructions" is a valid paraphrase, and "global instructions" correctly maps to ~/.claude/CLAUDE.md. Nuance the guide should reflect: auto memory is now ON by default (v2.1.59+) and captures vague "remember this" phrasing into a separate MEMORY.md, not CLAUDE.md — the word "instructions" is what steers Claude to CLAUDE.md, so "reliably" is slightly strong; naming CLAUDE.md explicitly is the surest phrasing. The old "#" shortcut is no longer the documented path (docs now cite only asking Claude directly, /memory, or /init). |
| F082 | wrong | high | The quote uses "outsource," not "delegate": "You can outsource your thinking, but you can't outsource your understanding." Karpathy popularized/cited it (his own tweet calls it "the quote I've been citing a lot recently") rather than originating it — the original is credited to another X user (@yacineMTB / "kache"). Safer framing: 'As Andrej Karpathy popularized it: "You can outsource your thinking, but you can't outsource your understanding."' | https://x.com/karpathy/status/2049907410303865030 | Two independent issues. (1) Wording: authoritative sources (Karpathy's own tweet and his Sequoia AI Ascent 2026 talk) consistently say "outsource," not "delegate." (2) Attribution: Karpathy's tweet frames it as a quote he cites, not one he coined; sources attribute the original to @yacineMTB. X fetches are somewhat unreliable, but the "outsource" wording is corroborated across Karpathy's tweet, the Sequoia talk summary, and multiple secondary write-ups, so the "delegate" wording is clearly wrong. |
| F083 | confirmed | high |  | https://code.claude.com/docs/en/best-practices | Anthropic's official Claude Code best-practices page states it almost verbatim: "Bloated CLAUDE.md files cause Claude to ignore your actual instructions!" and, under common failure patterns, "The over-specified CLAUDE.md. If your CLAUDE.md is too long, Claude ignores half of it because important rules get lost in the noise." Fix given: ruthlessly prune, and for each line ask whether removing it would cause mistakes. The claim's "buries the rules that matter" maps directly to "important rules get lost in the noise." |
| F084 | confirmed | high |  | https://code.claude.com/docs/en/skills | Official Claude Code docs state "Each skill is a directory with SKILL.md as the entrypoint," and the file tree marks SKILL.md as "Main instructions (required)" — exactly the guide's "folder with a SKILL.md text file inside." On the frontmatter the hint worried about: for Claude Code, none is strictly required — the reference table lists `name` as Required: No (defaults to the directory name) and `description` as Recommended (falls back to the first markdown paragraph), and the quickstart shows a working SKILL.md with only a description. So the simplification hides no hard requirement. (Note: the separate agentskills.io / platform.claude.com "Agent Skills" standard does treat name+description as required, but this guide teaches Claude Code, whose docs relax that.) |
| F085 | confirmed | high |  | https://code.claude.com/docs/en/skills | Official Claude Code Skills docs have an explicit "Where skills live" table: Personal = ~/.claude/skills/<skill-name>/SKILL.md (available across all projects); Project = .claude/skills/<skill-name>/SKILL.md (this project only). The quickstart also uses `mkdir -p ~/.claude/skills/summarize-changes`. Claim F085 is exactly correct. |
| F086 | confirmed | high |  | https://code.claude.com/docs/en/skills | Both authoritative docs support F086. Claude Code docs: "Claude uses skills when relevant, or you can invoke one directly with /skill-name" and "a skill's body loads only when it's used, so long reference material costs almost nothing until you need it." Platform docs' progressive-disclosure model: SKILL.md body loads only when the skill is triggered; only name+description (~100 tokens) load at startup, so "you can install many Skills without context penalty." Minor nuance: the tiny metadata blurb is loaded into every conversation, so unused skills carry a near-zero always-on cost — but the substantive body does not, matching the guide's meaning. |
| F087 | confirmed | high |  | https://code.claude.com/docs/en/skills | A skill is just a folder with a SKILL.md file, and Claude (a file-writing agent) creates both from a plain "turn this into a skill" request — this is standard behavior in Claude Code and Claude.ai. Anthropic also ships a dedicated skill-creator (bundled meta-skill in Claude.ai; an installable plugin, `/plugin install skill-creator@claude-plugins-official`, in Claude Code) that interviews you and generates the SKILL.md, confirming the described behavior. Minor nuance: the Claude Code docs' first-skill walkthrough demonstrates the manual mkdir + write-file path, and skill-creator on the Code side is documented mainly around the eval/iteration loop — but neither contradicts the claim as written for a beginner audience. |
| F088 | confirmed | high |  | https://code.claude.com/docs/en/skills | Official Claude Code docs confirm a skill is just a folder with SKILL.md; Claude Code watches skill directories and picks up an added skill in ~/.claude/skills/ within the session, no registration/enable step. Scripts use ${CLAUDE_SKILL_DIR} so paths aren't machine-specific — portable as claimed. Nuance: the docs' formal 'Share skills' section highlights project-repo commits, plugins, and managed settings for teams rather than 'mail the folder,' and creating a top-level skills dir that didn't exist at session start needs a restart — neither contradicts the beginner-level claim. |
| F089 | wrong | high | Code-05 should attribute the start of the habit to lesson 2 and the rule to lesson 4, e.g.: "Save outputs as files — you've done that since lesson 2's plan file, and as a standing rule since lesson 4." (Or minimally: "since lesson 2".) | /Users/alejo/best/work/writing/claude-onramp/04-drafts/code-05-compound.md | code-05 line 6 says the save-as-files habit dates "since lesson 4", but code-02 line 32 already instructs "Write your plan as a file in this folder before you start", and code-04 line 18's rule explicitly includes "the plan before it starts" as part of everything-becomes-a-file — so by the arc's own framing the habit begins in lesson 2 and lesson 4 only generalizes it into a rule. |
| F090 | confirmed | high |  | https://support.claude.com/en/articles/10166901-use-google-workspace-connectors | The official "Use Google Workspace connectors" Help Center page confirms the connectors are mostly read-only: Google Docs/Drive can only be read (no edit/modify of existing docs), Gmail can only create drafts, only Calendar has full create/update/delete. So "rewrite section three" is not possible via connector while "summarize" is — claim holds. One nuance: the docs describe the size constraint as "the documents must fit within the conversation's context window" rather than using the word "truncate"; the practical effect on long documents matches the claim, but if the guide wants to be precise it could say connectors read a limited slice / context-window-bounded amount rather than "truncate." |
| F091 | confirmed | high |  | https://github.com/LucaDeLeo/gdoc | The github.com/LucaDeLeo/gdoc repo is alive; its main-branch README describes itself as "A token-efficient CLI for AI agents to read, write, and collaborate on Google Docs." Docs editing works via the `edit` (find-and-replace, table-aware) and `write` (overwrite from markdown) commands. Sheets editing is real, not read-only: writes go through `gdoc cells` (single-cell/range writes, bulk CSV import via --file, piped --stdin, and formulas via --user-entered). No Slides support, but the claim only mentions Docs and Sheets, both of which it edits. |
| F092 | confirmed | high |  | https://gogcli.sh/ | gog (github.com/openclaw/gogcli, site gogcli.sh) is a single Go CLI for Google Workspace and explicitly covers Gmail, Calendar, and Drive (plus Docs, Sheets, Slides, Forms, Contacts, Tasks, admin, etc.). It's live and actively maintained — v0.32.0 released 2026-07-03, ~8.1k stars. The guide's framing (gdoc handles Docs/Sheets; gog does "everything else Google") is a slight simplification since gog itself also covers Docs/Sheets, but the specific claim that gog does Gmail, Calendar, and Drive is accurate. |
| F093 | unverifiable | medium |  | https://developers.google.com/workspace/guides/configure-oauth-consent | The claim is an experiential time estimate. The underlying console flow is real and documented (create a GCP project, enable Docs/Sheets/Drive/Gmail APIs, configure the OAuth consent screen, create OAuth client credentials, run the tool's auth flow), and Google describes the configuration steps as taking "only minutes"; for personal/testing use no Google verification is required. But no authoritative source gives a total wall-clock time for a novice walking this flow, so "fifteen minutes" can't be confirmed as stated. It is hedged with "maybe," sits at the top of the guide's own 5-15 min research range, and nothing contradicts it — so the estimate is reasonable and needs no change; it just isn't source-verifiable to the exact minute. |
| F094 | confirmed | medium |  | https://support.google.com/cloud/answer/15549945?hl=en | A Google Cloud project is free to create for anyone with a Google account, and Workspace APIs (Docs/Drive) need no billing account or credit card — that part is solidly true. "Nothing to maintain afterwards" is accurate for personal use, but only after the one-time consent-screen "In production" toggle that the draft itself correctly instructs: an External app left in "Testing" expires refresh tokens every 7 days (the gotcha the draft names). Once in production there's no key rotation and no quota worry for personal use; tokens persist as long as used within ~6 months. So the claim holds in context, but "nothing to maintain" is only literally true post-setup, not out of the box — worth keeping the two sentences tightly coupled so the phrase isn't read in isolation. |
| F095 | confirmed | high |  | https://developers.google.com/identity/protocols/oauth2 | Google's official OAuth 2.0 docs state that an app with an external OAuth consent screen in "Testing" publishing status is issued a refresh token that expires in 7 days (unless scopes are only a subset of name/email/profile). When that refresh token expires the tool loses its access and effectively gets logged out, matching the claim. Fix is to set the consent screen to "In production." The 7-day Testing expiry remains current per Google's docs. |
| F096 | confirmed | high |  | https://support.google.com/cloud/answer/15549945?hl=en | Google's official Cloud console help confirms both halves: (1) with publishing status "Testing," a test user's authorization — including offline refresh tokens — expires 7 days after consent; (2) switching the publishing status to "In production" removes that expiry. And no Google review is required for personal use: publishing to production does not trigger verification. An unverified app requesting sensitive scopes (which gdoc/gog do) just shows an "unverified app" warning you click through, and verification is only needed to remove that warning or exceed the 100-user lifetime cap — irrelevant for a solo user. Minor nuance the guide could note: in the current console the setting lives under the renamed "Google Auth Platform > Audience" page rather than a page literally called "consent screen," but the "In production" publishing-status value and the mechanism are exactly as stated. |
| F097 | confirmed | medium |  | https://super.engineering/ | super.engineering (Superconductor) blocked direct WebFetch with 403, but its own feature descriptions surfaced via search confirm every element: native macOS interface for running unlimited parallel agents; cross-repo/cross-project workspaces; running the same task across multiple models/worktrees in parallel; a built-in diff-review surface for viewing files; and macOS-native notifications when agents need attention (with 15 sounds). One small nuance: notifications fire "when agents need attention" (includes finishing/awaiting input), phrased as "when an agent finishes" in the claim — accurate enough. Consider softening "cross-project shortcuts" to "cross-project (cross-repo) workspaces," which is the site's actual framing. |
| F098 | wrong | high | Superconductor is not Mac only. Its download page offers macOS (Apple Silicon and Intel) and Linux (AppImage and .deb) desktop builds now, plus an iOS app; Windows is the only platform still marked "coming soon." | https://www.superconductor.com/download | super.engineering and the docs return 403 to fetch, but the official superconductor.com/download page lists active downloads for macOS (Apple Silicon + Intel), Linux (AppImage + .deb), and iOS, with only Windows "on the way." A web-search summary corroborated "macOS and Linux, Windows coming soon." So "Mac only for now" is inaccurate — Linux (and iOS) are already supported. Note: a separate unrelated product, SuperFlyTV/SuperConductor (a broadcast playout client), also exists; don't conflate. |
| F099 | confirmed | high |  | https://super.engineering/ | super.engineering's own site (surfaced via search) states Superconductor is "available for free download during the alpha period" and describes the macOS app as currently in alpha. The everydev.ai listing independently confirms it is listed as free and in alpha testing. The official site returns 403 to automated fetches and the interactive browser tool couldn't be used from a subagent, but the vendor's own wording came through search and settles it. No paid tiers are mentioned. |
| F100 | confirmed | high |  | https://super.engineering/ | super.engineering is the official site for Superconductor, the native macOS alpha app (free, Rust, isolated git worktrees) that runs your own Claude Code/Codex CLI agents locally with your own keys/subscription — matching the guide's description. Corroborated by the @superdoteng X account and the Mac App Store listing (id6749349238). superconductor.com (www.superconductor.com) is indeed a different company: a cloud multiplayer agent platform by Volition, Inc. The disambiguation is accurate and worth keeping, since that cloud product also brands itself "Superconductor" and also runs Claude agents, so a reader Googling the name could easily hit the wrong site. Note: super.engineering returns HTTP 403 to automated fetchers (bot-blocking), but it is live — confirmed via search indexing and the App Store/X presence. |
| F101 | confirmed | high |  | https://www.superconductor.com/ | Superconductor's own sites confirm the BYO model: agents (including Claude Code) run as native subprocesses using your existing plan/auth, nothing proxied or wrapped. super.engineering states agents "run as native subprocesses using your existing subscriptions" and never see your credentials; superconductor.com says "use your Claude Pro/Max plan... or your own API keys" with connected Claude plans respecting provider rate limits. Nuance: superconductor.com ALSO offers an optional hosted path ($20 free credits / API-key spend tracking) as an alternative to BYO, so "on your own subscription" is the documented default/supported mode rather than the only option. The claim as stated (own Claude Code, own subscription, no separate billing in that mode) is correct. Note: two products share the name "Superconductor" (super.engineering native macOS app; superconductor.com multiplayer workspace) — both use the BYO-subscription model, so the claim holds regardless of which the guide means. Could not directly fetch super.engineering (HTTP 403); relied on its search snippet plus the fetchable superconductor.com page. |
| F102 | confirmed | high |  | https://code.claude.com/docs/en/claude-code-on-the-web | Official docs confirm every part of F102: "Claude Code on the web runs tasks on Anthropic-managed cloud infrastructure at claude.ai/code. Sessions persist even if you close your browser, and you can monitor them from the Claude mobile app." It's web-based, so nothing to install. One nuance the guide draft (line 26) already handles but the claim omits: it's a research preview gated to paid plans — "Pro, Max, and Team users, and Enterprise users with premium seats or Chat + Claude Code seats." Not available on the free tier. The surface is named "Claude Code on the web," started at claude.ai/code. |
| F103 | confirmed | high |  | https://code.claude.com/docs/en/claude-code-on-the-web | Official Claude Code web docs confirm both halves of the claim. The page states cloud "sessions persist even if you close your browser, and you can monitor them from the Claude mobile app," and further: "open the session on claude.ai or the Claude mobile app to interact directly. From there you can steer Claude, provide feedback, or answer questions just like any other conversation." Note the feature is currently a research preview for Pro/Max/Team/eligible Enterprise users, but the mobile check-on-and-steer behavior is exactly as the draft describes. |
| F104 | confirmed | high |  | https://github.com/anthropics/claude-code | Claude Code launched Feb 2025 as a terminal/CLI tool (research preview), GA May 2025; IDE extensions and other surfaces came later. Anthropic's own GitHub and claude.com describe it as "an agentic coding tool that lives in your terminal." The CLI-first / "born in the terminal" framing is accurate. |
| F105 | confirmed | high |  | https://www.anthropic.com/news/introducing-claude-tag | Official Anthropic announcement (launched June 23, 2026) states Claude Tag is "available today in beta for Claude Enterprise and Team customers" and is not available on individual Pro/Max plans. All three parts of F105 — Team/Enterprise-only on Slack, still in beta, excluded from individual paid plans — match. Note: Anthropic says its goal is to "expand where it's available more widely," so the gating may loosen later, but it holds as of today. |
| F106 | confirmed | high |  | https://www.anthropic.com/news/introducing-claude-tag | Anthropic's official announcement page "Introducing Claude Tag" states it launched June 23, 2026, in beta for Claude Enterprise and Team customers — exactly matching the claim (June 2026, beta, Team/Enterprise plans). Corroborated by TechCrunch, Fortune, and VentureBeat coverage all dated June 23, 2026. If more precision is ever wanted, the specific date is June 23, 2026. |
| F107 | confirmed | high |  | https://www.anthropic.com/news/introducing-claude-tag | The claim is a behavior claim (async, server-side, keeps running while you're away), and the official Claude Tag announcement supports it: "Set Claude a task, and you can focus on your other priorities while it works. It can also schedule tasks for itself, pursuing a project autonomously over hours or days." Tag is an agent living inside Slack (it replaces the Claude-in-Slack app), so it is inherently server/cloud-hosted, not dependent on any user's laptop staying open — coverage describes it as a "persistent" teammate that "works autonomously." One nuance: the official page frames execution in terms of async/autonomous operation and doesn't use the literal word "cloud," but the guide's phrasing ("runs in the cloud — no laptop has to stay open") is an accurate plain-language rendering of the documented behavior. Also accurate in the surrounding draft: launched June 2026, beta, Team and Enterprise plans only. |
| F108 | confirmed | high |  | https://www.anthropic.com/news/introducing-claude-tag | Both official sources back the claim. Anthropic's launch post: "Within a given Slack channel, there's one Claude that interacts with everyone" and "As Claude follows along with its channel, it builds more context about the work... users don't need to explain things to it from scratch over and over again." The support page (support.claude.com/en/articles/15594475) adds that "Claude Tag remembers context per channel and per workspace" and that channel/workspace memory is retained across tasks (admins can view/edit/delete it under Org settings > Claude Tag > Audit). Memory is scoped to the channel unless an admin grants cross-channel access. The "months of team decisions in its head" phrasing is accurate framing of accumulated per-channel memory, so an established channel skipping the briefing is correct. |
| F109 | confirmed | high |  | https://www.anthropic.com/news/introducing-claude-tag | Anthropic's official Claude Tag announcement states "As Claude follows along with its channel, it builds more context about the work" — i.e., it builds channel memory from what it observes in a channel, so a channel dedicated to one kind of work becomes accumulated context/training about that team's work. Memories stay scoped to admin-defined channels (a sales Claude won't pass memory to an engineering one), which reinforces that per-channel learning is the mechanism. The guide's phrasing ("learns from what it can see," "becomes its training on your team") is an accurate lay paraphrase of the documented behavior. |
| F110 | confirmed | high |  | https://claude.com/resources/tutorials/best-practices-using-claude-tag | Both official Claude Tag pages confirm the claim as stated. The best-practices tutorial says verbatim "Each top-level message starts a new task for Claude" and "Editing a message you already sent has no effect; send a new reply instead." The how-it-works concept page corroborates: "Editing or deleting a message in the thread does not change what Claude has already received," and directs users to steer a running session by replying in the thread. Matches the appendix's phrasing exactly. |
| F111 | confirmed | high |  | https://claude.com/resources/tutorials/best-practices-using-claude-tag | Official Claude Tag docs confirm the behavior. The best-practices tutorial states: "corrections and preferences you give to Claude stick. When a result comes back, tell it what you liked or what you'd change, and it saves that for the channel going forward," and "Claude remembers the correction for the channel, so the next person doesn't hit it." The memory doc (claude.com/docs/claude-tag/users/memory) corroborates: corrections/preferences given in-channel persist to the channel's memory store, scoped to that channel (public-channel memory is shared workspace-wide). One nuance worth keeping in the guide's back pocket: when the fix is to overwrite an existing stale memory entry, the docs recommend explicitly telling Claude to "update your memory for this channel," but for ordinary corrections after a result the persistence is described as automatic — so the claim as stated is supported. |
| F112 | confirmed | high |  | https://www.anthropic.com/news/introducing-claude-tag | Anthropic's official Claude Tag announcement confirms the behavior: an opt-in setting lets Claude act proactively and run standing duties without being tagged. Exact wording on the official page is "ambient" behavior/setting ("If 'ambient' behavior is enabled, Claude will proactively keep you updated..."); "ambient mode" is the common shorthand used by third-party coverage. The guide's phrasing is accurate; if strict fidelity to Anthropic's wording is wanted, "ambient" or "ambient behavior/setting" is the exact term, but "ambient mode" is fine. |
| F113 | confirmed | high |  | https://www.anthropic.com/news/introducing-claude-tag | Anthropic's official Claude Tag announcement states almost verbatim: "Think of it as creating separate Claude identities for different uses: everything, including its memories, will stay scoped to the channels defined by the administrators. For example, a model set up for sales work won't pass on memories to one set up for engineering; nor will it give engineers access to any sales data or tools." The support.claude.com doc frames the mechanism as channel/workspace-scoped memory and a hierarchical permission model (org-wide → workspace → private channel), and notes cross-channel access is possible if an admin explicitly grants it — so "never cross" is true by default but is admin-configurable, not an absolute hard guarantee. The guide's phrasing is a fair paraphrase of Anthropic's own framing and is accurate as stated. |
| F114 | confirmed | high |  | https://support.claude.com/en/articles/15594475-what-is-claude-tag | Anthropic's Claude Tag docs describe an Audit view at "Organization settings > Claude Tag > Audit" that lists every scheduled and one-time task across the org plus all network calls made using Agent Identity (the outbound tool calls). Admin materials also state admins can "view a log of everything that @Claude has done, along with who requested each task," covering the requester. All three elements of the claim check out; only minor terminology nuance (outbound tool call = "network call made using Agent Identity"). Fetched the support article and the agent-identity doc; the support article is authoritative on the location and contents. |
| F115 | unverifiable | medium |  | https://support.claude.com/en/articles/11506255-get-started-with-claude-in-slack | No Anthropic source documents that asking Tag "what can you access from this channel?" returns its connections. I fetched the "Get started with Claude in Slack" support article and the "Introducing Claude Tag" announcement: both describe that admins configure which channels/tools/data each Claude identity can access and that there's an audit log, and that Slack surfaces "provide the same capabilities you have enabled... including web search and connections to your integrated tools" — but neither describes this self-introspection prompt or guarantees it lists connections. It's a plausible generic LLM behavior (tools sit in context, so Claude can often describe them), but not documented, and I can't live-test it without a Team/Enterprise Slack workspace with Tag enabled. Suggest softening the guide's phrasing to a general "ask it what it can see" tip rather than a documented feature, or drop the specific prompt wording. |
| F116 | confirmed | high |  |  | Traced every pointer: cowork-04's lesson 1/2/3 citations match cowork-01 (virtual machine), cowork-02 (dictation), cowork-03 (small test); cowork-05's lesson-1 and front-door citations match cowork-01's save-as-files instruction and front-door.md's "same Claude, different fences" and "Linux, not Windows" lines; code-02's "Yesterday you installed it" matches code-01-install; code-06's "run-many lesson" and "first lesson" match code-04-run-many and code-01's skipped terminal install; appendix-tag's front-door quote matches front-door.md line 14. No URL — this is an internal-consistency check settled by files under /Users/alejo/best/work/writing/claude-onramp/04-drafts/. |
